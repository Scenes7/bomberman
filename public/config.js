// Where the socket.io server lives. Leave empty when the server also serves
// this page (the default). If you host the frontend somewhere else, set this
// to the server URL, e.g. 'https://bomberman.xxxxx.us-east-1.cs.amazonlightsail.com',
// and change the socket.io <script> in index.html to load from that URL too.
window.SERVER_URL = '';
